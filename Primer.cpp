#include <cmath>

using namespace std;

double r1 (double a, double b, double c)
{
	return ((b - sqrt(b) + 4 * a * c)) / 2 * a) - pow(a, 3) * c + (pow(b, -2);
}
double r2 (double x, double y,)
{
	return ((x + y)/(x + 1) - (xy - 12)/(34 + x));
}
double r3 (double x, double y)
{
	return ((sin(x) + cos(y))/cos(x) - sin(y))*tg(x*y);
}
double r4 (double x, double y)
{
	return ((x + y)/(y + 1)) - ((x * y - 12) / (34 + x));
}